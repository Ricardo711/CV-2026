the code in app.py is just a demo of the main idea od the project. The bottoms to click next will have to be pressed two times to continue with the interaction.

To run the code in terminal run:
python -m streamlit run app.py